// Este archivo incluye el código Rust generado automáticamente por tonic-build
// a partir de los archivos .proto.

pub mod wallet {
    // Busca el archivo generado a partir de "wallet.proto" en el directorio de salida (OUT_DIR)
    tonic::include_proto!("wallet");
}
